import { ThermostatPipe } from './thermostat.pipe';

describe('ThermostatPipe', () => {
  it('create an instance', () => {
    const pipe = new ThermostatPipe();
    expect(pipe).toBeTruthy();
  });
});
