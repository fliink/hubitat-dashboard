import { ColorAveragePipe } from './color-average.pipe';

describe('ColorAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
